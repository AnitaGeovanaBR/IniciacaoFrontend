# CalculadoraApi (backend)

Endpoint:
- GET /calculadora/operacoes — retorna um array JSON de operações

dotnet restore
dotnet run --urls "http://localhost:5000"
How to run (PowerShell):

```powershell
rode o backend na pasta CalculadoraApi '\IniciacaoFrontend-feature2\backend\CalculadoraApi'
# veja se o  .NET 10 SDK está instalado 
# dotnet dev-certs https --trust
dotnet restore
dotnet run --urls "http://localhost:5000"
```